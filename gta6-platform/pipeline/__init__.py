"""GTA VI Platform — automated content pipeline.

Every stage is pure Python (stdlib only). The pipeline is idempotent:
running it twice with no new discoveries produces the same output.
"""

__version__ = "0.1.0"
