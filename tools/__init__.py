"""Operator tools.

A package only so the pure functions here can be unit tested directly. Every
tool remains runnable as a script (`python tools/<name>.py`); nothing in the
desk imports from this package at runtime.
"""
